const Searchbar = () => {
  return (
    <div className="container mx-auto flex">
      <input type="text" placeholder="Sök annons..." />
      <input type="text" />
    </div>
  );
};

export default Searchbar;
